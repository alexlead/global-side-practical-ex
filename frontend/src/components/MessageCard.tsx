import React from 'react';

interface IMessageCardProps {
}

const MessageCard: React.FunctionComponent<IMessageCardProps> = (props) => {
    return (
        <>

        </>
    );
};
export default MessageCard;